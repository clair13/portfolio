<div class="album text-muted">
  <div class="container">
    <div class="row <%= "sortable" if logged_in?(:site_admin) %>">
      <%= render partial: 'port_item', collection: @port_items %>
    </div>
  </div>
</div>